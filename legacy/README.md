# Legacy

이 폴더는 `apps/simple_mlops` 중심으로 repository를 재구성하면서 보존한 이전 course 자료입니다.

새 작업의 기준은 repository root와 `apps/simple_mlops`입니다. 이전 labs, package, artifact, GitOps, notebook 자료에서 필요한 코드가 생기면 이 폴더에서 확인한 뒤 새 구조로 다시 끌어올립니다.
