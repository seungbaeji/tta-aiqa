apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: risk-api
  namespace: tta-aiqa
  labels:
    app.kubernetes.io/name: risk-api
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: risk-api
                port:
                  name: http
