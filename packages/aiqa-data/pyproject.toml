[project]
name = "aiqa-data"
version = "0.2.0"
description = "PhysioNet data preparation bounded context for AIQA V2"
requires-python = ">=3.11,<3.12"
dependencies = [
  "aiqa-core",
  "numpy>=1.26,<2.3",
  "pandas>=2.2",
  "pydantic>=2.7",
  "pyyaml>=6.0",
  "scikit-learn>=1.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aiqa_data"]
