[project]
name = "aiqa-data"
version = "0.1.0"
description = "Dataset preparation for the AIQA Simple MLOps workspace"
requires-python = ">=3.11"
dependencies = [
  "aiqa-core",
  "pandas>=2.2",
  "scikit-learn>=1.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aiqa_data"]
